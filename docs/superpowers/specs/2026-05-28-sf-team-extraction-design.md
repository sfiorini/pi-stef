# SF-Team Extraction Design

Extract the fh-team agent framework and its 4 sibling dependencies from the fh-agent monorepo into pi-stef as `@pi-stef/*` packages. Rename all fh/first-horizon references to sf-team equivalents. Fix the pi API namespace in superpowers-adapter.

## Scope

5 packages from `fh-agent/packages/` → `pi-stef/packages/`:

| Source | Destination | New scope |
|--------|-------------|-----------|
| `fh-team` | `sf-team` | `@pi-stef/sf-team` |
| `agent-workflows` | `agent-workflows` | `@pi-stef/agent-workflows` |
| `atlassian` | `atlassian` | `@pi-stef/atlassian` |
| `figma` | `figma` | `@pi-stef/figma` |
| `web-access` | `web-access` | `@pi-stef/web-access` |

## Renaming Taxonomy

### Category A — Scope rename (all 5 packages)

`@life-of-pi/*` → `@pi-stef/*` in package.json names, imports, and `file:` links.

### Category B — fh→sf rename (sf-team package only)

| Pattern | Replacement | Context |
|---------|-------------|---------|
| `fh_team_*` | `sf_team_*` | Tool names, slash commands |
| `fh-team` | `sf-team` | Identifiers, docs, config paths |
| `fh_team` | `sf_team` | snake_case identifiers |
| `fhTeam` | `sfTeam` | camelCase identifiers |
| `FhTeam` | `SfTeam` | PascalCase identifiers |
| `fh-team.ts` | `sf-team.ts` | File renames |
| `.fh-team.json` | `.sf-team.json` | Config file names |
| `~/.pi/fh-team/` | `~/.pi/sf-team/` | Global config paths |
| "First Horizon" | removed | Descriptions, comments |
| `fh-agent.package.json` | removed | Pi-stef uses `pi` field in package.json |

### Category C — API namespace fix (superpowers-adapter)

`@mariozechner/pi-*` → `@earendil-works/pi-*` in imports and peerDependencies.

## File-Level Renames in sf-team

- `extensions/fh-team.ts` → `extensions/sf-team.ts`
- `fh-agent.package.json` → removed
- `config/defaults.json` — update paths and fh-team references
- `bin/notify-telegram.sh` — update any fh references
- `scripts/pretty-pane.mjs` — update any fh references
- `skills/team/*.yaml` — update fh references in planner/reviewer/developer prompts
- `templates/*.md` — update fh references in continuation-runbook, milestone-plan, story-tracker, task-plan
- All test files updated for sf-team naming

## Root-Level Changes in pi-stef

| File | Change |
|------|--------|
| `tsconfig.json` | Add references for all 5 new packages |
| `README.md` | Add rows for all 5 packages in the packages table |
| `scripts/install-all.sh` | Add new package names to PACKAGES array |
| `pnpm-workspace.yaml` | No change (already uses `packages/*` glob) |
| `vitest.config.ts` | No change (already discovers `packages/*/tests/**/*.test.ts`) |

## Dependency Chain

```
@pi-stef/sf-team
  ├── @pi-stef/agent-workflows  (standalone, no sibling deps)
  ├── @pi-stef/atlassian
  │     └── @pi-stef/figma      (cross-dep preserved)
  ├── @pi-stef/figma
  └── @pi-stef/web-access       (standalone, no sibling deps)
```

`file:` protocol links updated to relative paths within pi-stef's workspace.

## What Not To Change

- Actual logic, algorithms, or control flow
- External package names (marked, typebox, zod, playwright-core, etc.)
- `package-lock.json` — regenerated by pnpm

## Approach

Bulk copy all 5 packages, then systematic find-and-replace in three passes (scope, fh→sf, API namespace), followed by typecheck and test verification.

## Success Criteria

- `pnpm typecheck` passes at root
- `pnpm test` passes at root
- No occurrences of `fh-team`, `fh_team`, `@life-of-pi`, `First Horizon`, or `@mariozechner/pi-*` remain in the codebase
- Root README lists all 6 packages (superpowers-adapter + 5 new)
