// AUTO-GENERATED scraped context-window lookup from cursor.com/docs/models-and-pricing.
// Regenerate via: pnpm --filter @pi-stef/cursor refresh-models  (requires chromium: npx playwright install chromium)
// MANUAL — not run in CI

// Self-contained inline type (NO import from model-config.ts — avoids forward-ref;
// structurally identical to ScrapedContextEntry defined in S-M1-2).
export const SCRAPED_MODEL_CONTEXTS: Record<
  string,
  { contextWindow?: number; maxContext?: number; slug: string }
> = {};
